{
  "name": "azuac-bot-widget",
  "version": "1.0.0",
  "description": "Production-ready embeddable chatbot widget",
  "scripts": { "build": "node build.js", "dev": "node build.js --watch" },
  "devDependencies": { "esbuild": "^0.21.0" }
}
